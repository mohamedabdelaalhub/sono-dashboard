# لوحة مؤشرات Swnw — تعليمات المشروع

> اقرأ هذا الملف كاملاً قبل أي تعديل. فيه قرارات وأخطاء
> كلّفت وقتاً حتى اكتُشفت — لا تكرّرها.

---

## ما هذا المشروع

لوحة تحليل مالي وتشغيلي لـ **مركز عيادات Swnw التخصصية — فرع حدائق الأهرام**.

المستخدم يرفع ملفات إكسل (خزينة · تقارير أطباء · خدمات · جدول عيادات)
فتُخرج اللوحة: ملخصاً · مؤشرات · مخاطر · توصيات · خطة عمل · تحليلاً ذكياً.

منشورة على GitHub Pages:
`https://mohamedabdelaalhub.github.io/sono-dashboard/`

مشروع منفصل تماماً عن `hris/` (نظام الموارد البشرية). لا يشتركان في شيء.

---

## المستخدم

**محمد عبدالعال — غير تقني.** لذلك:

- لا تعطه أوامر Terminal يكتبها بنفسه
- كل عملية تُسلَّم كملف `.command` بالعربي يفتحه بالدبل كليك
- الشرح بالعربية المصرية، خطوة خطوة
- عند أي خطأ: اطلب صورة الشاشة بدل الوصف

---

## البنية

موقع ثابت بالكامل — HTML/CSS/JavaScript عادي. **لا يوجد build ولا npm.**

كل ملف JS نمط IIFE يعلّق نفسه على `window.Sono*`:

```
config.js            إعدادات المركز + مفاتيح Supabase  ⚠️ لا تلمسه
assets/js/
  brand.js           الشعار والألوان
  auth.js            الدخول عبر Supabase
  parser.js          قراءة الإكسل
  parser-auto.js     كشف نوع التقرير تلقائياً
  parser-status.js   تقارير الحالة
  parser-pdf.js      قراءة PDF
  reports.js         سجلّ 31 نوع تقرير (تعريفات)
  adapters.js        تحويل كل تقرير لصيغة موحّدة
  insights.js        محرّك التحليل — محلّل لكل تقرير
  analytics.js       الحسابات المالية
  rules.js           محرّك القواعد (المخاطر والتوصيات)
  schedule.js        قراءة جدول العيادات
  schedule-store.js  حفظ الجدول المرجعي
  charts.js          الرسوم (SVG يدوي)
  render*.js         العرض
  exporters.js       تصدير PDF و Excel
  ai.js              التحليل الذكي
  archive.js         الأرشيف
  compare.js         المقارنة بين فترات
```

مكتبات من CDN: SheetJS 0.18.5 · jsPDF 2.5.1 · html2canvas 1.4.1 · pdf.js 3.11.174

---

## ⚠️ قواعد لا تُكسر

### 1) `config.js` — لا تكتب فوقه أبداً

فيه رابط Supabase ومفتاحه الحقيقيَّين. لو اتغيّر بالخطأ **يقف الدخول للوحة**.
عدّل فيه سطراً بعينه عند الحاجة، ولا تعِد كتابة الملف كله.

### 2) بصمة الكاش — ارفعها مع كل تعديل

كل وسوم `<script>` و `<link>` في `index.html` تحمل `?v=N`.
**بعد أي تعديل على JS أو CSS ارفع الرقم** وإلا رأى المستخدم النسخة القديمة.

الرقم الحالي: **`?v=10`**

```bash
sed -i '' 's/?v=10/?v=11/g' index.html
```

وبعد الرفع قل له يضغط `Command + Shift + R`.

### 3) خطوط الهوية — لا خطوط Google إطلاقاً

- **BigVesta Arabic** لكل العربي
- **Hiragino Kaku** للاتيني والأرقام

الملفات محلّية في `assets/fonts/` وهي WOFF2 (17–27 KB).
الترتيب دائماً هكذا، والأرقام تأتي من Hiragino عبر `unicode-range`:

```css
font-family:"Hiragino Kaku","BigVesta Arabic",system-ui,Tahoma,sans-serif;
```

❌ Cairo · IBM Plex · Tajawal · أي خط من Google Fonts — **مُنعت وأُزيلت**،
لأنها كانت تُحمَّل من النت وتغطّي على خطوط البراند.

### 4) الألوان

| الاستخدام | الكود |
|---|---|
| الأساسي | `#0F369D` |
| التنبيه | `#F15A22` |
| النص الداكن | `#16212E` |
| الإيجابي | `#2F7D5C` |
| السلبي | `#D0402A` |

### 5) الاسم

بالإنجليزي **`Swnw`** دائماً — لا `Sono` في أي نص يراه المستخدم.
أسماء المتغيّرات القديمة (`SonoBrand`, `SONO_CONFIG`, مجلد `sono-dashboard`،
اسم المستودع) تُترك كما هي — تغييرها يكسر رابط النشر.

### 6) الخصوصية

❌ لا تُخزَّن ولا تُعرض ولا تُرسَل أسماء المرضى أو أرقامهم أو تشخيصاتهم.
الأرشيف يحفظ `unclassifiedSummary {count, total}` فقط —
هذا القيد جاء بعد تسرّب فعلي لـ 9 أسماء مرضى في نسخة سابقة.

---

## أخطاء وقعت فعلاً — لا تكرّرها

### دالة غير معرَّفة تُسقط الصفحة كلها بصمت

*(وقع هذا في مشروع `hris/` الشقيق، والدرس ينطبق هنا حرفياً.)*

أُضيف عنصر في جدول يستدعي دالة لم تُعرَّف، فتوقّف العرض عند أول صف
وظهرت رسالة **«تعذّر الاتصال بقاعدة البيانات»** — مضلِّلة تماماً،
فالاتصال كان سليماً والعطل في الصفحة نفسها.

**درسان:**

1. أي دالة جديدة تُستدعى داخل العرض — تأكّد من تعريفها، واختبر **بعد** التعديل
2. رسالة الخطأ في `catch` يجب أن تصف ما حدث فعلاً لا ما تظنّه —
   وإلا أرسلتَ المستخدم يبحث في الاتجاه الخاطئ

### `draw()` بلا مفتاح للتاب

`exportPdf('all')` كان ينهار بـ `{…}[t] is not a function`
لأن خريطة `draw()` لم يكن فيها مفتاح `rep`.
**أي تاب جديد يجب أن يُضاف للخريطة**، ومعها حارس `if (!f) return;`.

### NaN في التقارير

`DoctorClaimReport` أخرج `NaN` لأن الإيراد صفر والتكلفة موجبة.
الشرط الصحيح `A.kpi.revenue > 0` وليس `revenue > 0 || cost > 0`،
وكل نسبة تمرّ على `pc = v => (isFinite(v) ? … : '0.0') + '%'`.

### تطبيع الأسماء العربية

`key()` في `schedule.js` كان يحذف حرف **و** فيتحوّل «جدول» إلى «جدل»
فلا تُطابَق ورقة الإكسل أبداً. **احذف المسافات والشرطات فقط.**

### أعمدة الجدول اليدوي

بداية كل بلوك تُكتشف من خلايا عنوان **«التخصص»** لا من أسماء الأيام في الصف 0
(لأنها تقع بعمود واحد لليمين)، مع إزاحة `off` وحارس `price >= 50`.
الملف الأصلي فيه خطأ إملائي `النخصص` — مُعالَج في `HEAD_LABELS`.

### الاختبار من غير متصفح

الساندبوكس بلا صلاحيات لتثبيت Playwright. الاختبار يتم بـ **jsdom**:

```bash
node /tmp/test.js   # تحميل index.html + محاكاة fetch + فحص DOM
```

⚠️ عند فحص النص المعروض استخدم نسخة من `body` بعد حذف `<script>` —
وإلا طابقتَ كلماتٍ موجودة داخل كود JS نفسه وحصلت على نتيجة خاطئة.

---

## الرفع

المستخدم يرفع بملف `رفع-الداشبورد.command` في المجلد الأب (دبل كليك).
لا ترفع أنت — الساندبوكس بلا صلاحيات GitHub.

الترتيب: عدّل → ارفع بصمة الكاش → اختبر → قل له يشغّل ملف الرفع
→ ثم `Command + Shift + R`.

---

## قبل أي تعديل

1. تأكّد أن الملفات القديمة تعمل (`git status` نظيف أو مفهوم)
2. اشتغل على الإضافة وحدها — لا تعِد صياغة ما يعمل
3. اختبر أن **الـ 31 تقريراً والتصدير وجدول العيادات** لا تزال تعمل
4. ارفع بصمة الكاش
5. سلّمه ملف الرفع

---

## حالة المشروع

**يعمل:** 31 نوع تقرير كلها تشغّل اللوحة بتاباتها كاملة · تصدير PDF و Excel ·
تاب جدول العيادات (47 طبيباً · 18 تخصصاً · صفر مجهول) · الدخول عبر Supabase ·
الأرشيف · المقارنة بين الفترات · خطوط وألوان الهوية

**فجوات معروفة:**

- عمود التكلفة في `ServiceReport` أصفار في كل الـ 1567 صفاً،
  فهامش الربح لكل خدمة غير ممكن حسابه
- ألقاب لم تُطابَق بأسماء كاملة: `panda` · `nasser` · `om youssef` · `ahmed`
  و `nasser` غير موجود ضمن الـ 19 اسماً أصلاً
- لا يوجد ربط بين اللوحة و `hris/` — أقرب نقطة تلاقٍ هي الأطباء الـ 47
