# دليل النشر — من جهازك إلى لينك أونلاين

الوقت المتوقع: **١٥ دقيقة** (٥ دقائق للرفع + ١٠ لإعداد الحماية).

---

# الجزء الأول — الرفع على GitHub والنشر

## ١) أنشئ حساب GitHub

لو عندك حساب، تخطَّ هذه الخطوة. غير كده: [github.com/signup](https://github.com/signup)

## ٢) أنشئ مستودعاً جديداً

1. من [github.com/new](https://github.com/new)
2. **Repository name**: `sono-dashboard`
3. اختر **Private** (مهم — التقرير يحتوي أرقام مركزك)
4. **لا** تفعّل أي خيار من الثلاثة تحت (README / .gitignore / license)
5. اضغط **Create repository**

> **Private مع GitHub Pages**: النشر من مستودع خاص يحتاج خطة GitHub Pro.
> لو حسابك مجاني، عندك خياران:
> - اجعل المستودع **Public** — لا مشكلة أمنية طالما فعّلت Supabase وحذفت أي بيانات حقيقية من الريبو (اللوحة لا تحتوي بيانات، البيانات ترفعها أنت وقت الاستخدام).
> - أو انشر على **Cloudflare Pages** المجاني — راجع «بديل أفضل» في نهاية الملف.

## ٣) ارفع الملفات

### الطريقة أ — بالسحب والإفلات (بدون أوامر)

1. في صفحة المستودع الجديد اضغط **uploading an existing file**
2. افتح مجلد `sono-dashboard` على جهازك
3. اسحب **محتويات** المجلد (وليس المجلد نفسه): `index.html`، `config.js`، `README.md`، `DEPLOY-AR.md`، `.nojekyll`، ومجلدَي `assets` و`supabase`
4. اكتب في **Commit changes**: `أول إصدار`
5. اضغط **Commit changes**

> لو المتصفح لم يقبل سحب المجلدات، اسحب الملفات المفردة أولاً، ثم افتح
> **Add file → Upload files** مرة أخرى واسحب مجلد `assets` وحده.

### الطريقة ب — بالأوامر (أسرع لو عندك Git)

```bash
cd مسار/sono-dashboard
git init
git add .
git commit -m "أول إصدار"
git branch -M main
git remote add origin https://github.com/USERNAME/sono-dashboard.git
git push -u origin main
```

استبدل `USERNAME` باسم حسابك.

## ٤) فعّل GitHub Pages

1. في المستودع: **Settings** (أعلى الصفحة)
2. من القائمة الجانبية: **Pages**
3. تحت **Build and deployment → Source** اختر **Deploy from a branch**
4. **Branch**: `main` · **Folder**: `/ (root)`
5. **Save**

انتظر دقيقة إلى ثلاث. سيظهر أعلى الصفحة:

```
Your site is live at https://USERNAME.github.io/sono-dashboard/
```

هذا هو لينكك. افتحه وجرّب الدخول بـ:

- البريد: `mohamadmh32@gmail.com`
- كلمة السر: `Sono@2026`

> **مهم**: هذه كلمة سر افتراضية وضعيفة، والوضع المحلي ليس حماية حقيقية.
> انتقل فوراً للجزء الثاني.

## ٥) تحديث اللوحة لاحقاً

**من المتصفح**: افتح الملف في GitHub → أيقونة القلم ✏️ → عدّل → **Commit changes**.
**بالأوامر**: `git add . && git commit -m "تحديث" && git push`

التغيير يظهر خلال دقيقة تقريباً. لو لم يظهر، اضغط `Ctrl+Shift+R` لتفريغ الكاش.

---

# الجزء الثاني — الحماية الحقيقية بـ Supabase

الوضع المحلي يمنع الفضولي فقط. Supabase يعطيك تسجيل دخول فعلياً، مع إدارة أدمنز وإيقاف أي حساب متى شئت. مجاني بالكامل لهذا الاستخدام.

## ١) أنشئ مشروعاً

1. سجّل في [supabase.com](https://supabase.com) بحساب GitHub
2. **New project**
3. **Name**: `sono` · **Database Password**: ولّد كلمة قوية واحفظها
4. **Region**: اختر الأقرب (Frankfurt أو London مناسبان لمصر)
5. **Create new project** — انتظر ~٢ دقيقة

## ٢) انسخ مفاتيح الاتصال

**Project Settings** (الترس) **→ API**:

- **Project URL** — مثل `https://abcdefgh.supabase.co`
- **anon public** — مفتاح طويل يبدأ بـ `eyJ…`

> مفتاح `anon` **آمن** في كود الواجهة — هذا استخدامه المقصود، وحمايته الفعلية من سياسات RLS.
> لا تضع أبداً مفتاح `service_role` في أي ملف تنشره.

## ٣) جهّز قاعدة البيانات

1. من القائمة الجانبية: **SQL Editor → New query**
2. افتح ملف `supabase/setup.sql` وانسخ محتواه كاملاً والصقه
3. **قبل التشغيل**: عدّل البريد والاسم في جملة `insert` بالأسفل ليطابق بريدك
4. اضغط **Run**

## ٤) أنشئ حسابك

1. **Authentication → Users → Add user → Create new user**
2. البريد وكلمة سر قوية
3. **فعّل خيار Auto Confirm User** (مهم، وإلا لن تستطيع الدخول)
4. **Create user**
5. ارجع إلى **SQL Editor** وشغّل جملة الـ `insert` من الملف مرة أخرى (الآن سيجد المستخدم ويربطه)

للتأكد، شغّل:

```sql
select email, name, role, active from public.admins;
```

يجب أن يظهر سطرك بدور `مالك`.

## ٥) فعّل الوضع في اللوحة

عدّل `config.js`:

```js
authMode : 'supabase',

supabase : {
  url     : 'https://abcdefgh.supabase.co',
  anonKey : 'eyJhbGciOi…'
},
```

احفظ وارفع التعديل (`Commit changes`). أعد فتح اللوحة — الآن تسجيل الدخول حقيقي.

## ٦) اضبط النطاقات المسموحة

**Authentication → URL Configuration**:

- **Site URL**: `https://USERNAME.github.io/sono-dashboard/`
- تحت **Redirect URLs** أضف نفس اللينك

## ٧) أضف باقي المستخدمين — من اللوحة نفسها، بدون SQL

من هنا فصاعداً لن تحتاج فتح Supabase مرة أخرى.

1. سجّل الدخول إلى اللوحة بحسابك
2. اضغط **⚙** في شريط أعلى الصفحة
3. تاب **المستخدمون والصلاحيات**
4. اكتب البريد والاسم، واختر الدور، ثم **إضافة**
5. أرسل له رابط اللوحة وقل له: افتح **«حساب جديد»** واستخدم نفس البريد وأنشئ كلمة سرك

**لا ترسل له كلمة سر** — هو ينشئها بنفسه، وأنت لا تعرفها.

من نفس الجدول تقدر:

- **تغيير الدور** من القائمة المنسدلة مباشرة
- **إيقاف** حساب مؤقتاً دون حذفه
- **حذف** المستخدم نهائياً

العمود الأخير يوضّح حالة كل شخص: «بانتظار إنشاء الحساب» أو «مفعّل».

> آخر سوبر أدمن نشط محميّ — لا يمكن إيقافه ولا تغيير دوره، حتى لا تقفل نفسك خارج اللوحة.

## ٨) مفتاح الذكاء الاصطناعي (اختياري)

اللوحة تعمل كاملة بدونه. المخاطر والتوصيات وخطة العمل كلها من محرك القواعد المدمج — مجاني وفوري.

المفتاح يضيف فوقها: **قراءة تنفيذية مكتوبة** و**شات تسأله بالعامية** عن أرقامك.

1. احصل على مفتاح من [Anthropic Console](https://console.anthropic.com/settings/keys) أو [OpenAI](https://platform.openai.com/api-keys)
2. في اللوحة: **⚙ ← إعدادات الذكاء الاصطناعي**
3. اختر المزوّد والنموذج، الصق المفتاح، ثم **اختبار الاتصال**
4. لو ظهر «الاتصال ناجح ✓» اضغط **حفظ الإعدادات**

**من يستخدمه؟** أنت وحدك افتراضياً. لتشغيله لباقي الفريق فعّل **«السماح لباقي المستخدمين»** — لن يروا المفتاح، لكن استهلاكهم على رصيدك.

**ما الذي يخرج من جهازك؟** أرقام مجمّعة فقط: الإيرادات والمصروفات والنسب وفئات الخدمات والمخاطر. أسماء الأطباء اختيارية بمفتاح في نفس الصفحة. **أسماء المرضى وأرقام ملفاتهم لا تُرسل إطلاقاً.**

> التكلفة بالاستخدام على حسابك لدى المزوّد. القراءة الواحدة تكلّف عادةً أقل من جنيه.

---

# بديل أفضل: Cloudflare Pages

يعطيك مستودعاً خاصاً + نشراً مجانياً + إمكانية قفل الموقع كله بكلمة سر.

1. سجّل في [dash.cloudflare.com](https://dash.cloudflare.com)
2. **Workers & Pages → Create → Pages → Connect to Git**
3. اربط حساب GitHub واختر `sono-dashboard`
4. **Framework preset**: `None` · **Build command**: اتركه فارغاً · **Output directory**: `/`
5. **Save and Deploy**

ستحصل على `https://sono-dashboard.pages.dev`

**لقفل الموقع كله بطبقة إضافية**: Cloudflare **Zero Trust → Access → Applications → Add
an application → Self-hosted**، وجّهه إلى نطاقك، واختر سياسة **Emails** وأضف الإيميلات
المسموح لها. هكذا لا أحد يصل حتى لصفحة الدخول إلا بعد التحقق من بريده.

---

# حل المشاكل

| المشكلة | السبب والحل |
|---|---|
| صفحة بيضاء بعد النشر | افتح Console (F12) وشاهد الخطأ. غالباً ملف في `assets` لم يُرفع — راجع بنية المجلدات في المستودع |
| «تعذّر الاتصال بخادم المصادقة» | راجع `url` و`anonKey` في `config.js`، وتأكد أن المشروع في Supabase نشط |
| «هذا البريد غير مُصرّح له بالدخول» | لم يُضَف من ⚙ لوحة التحكم. أضفه أولاً ثم اطلب منه المحاولة |
| «هذا البريد غير مُضاف من مدير المركز» | نفس السبب — الإضافة تسبق إنشاء الحساب دائماً |
| «جدول الأدمنز غير موجود» | لم تشغّل `supabase/setup.sql` بعد |
| «المفتاح غير صحيح أو منتهٍ» | راجع المفتاح لدى المزوّد، أو ولّد واحداً جديداً |
| «اسم النموذج غير صحيح» | غيّر النموذج من ⚙ ← إعدادات الذكاء الاصطناعي |
| «تجاوزت حد الاستخدام أو الرصيد» | اشحن رصيدك لدى Anthropic أو OpenAI |
| «Email not confirmed» | لم تفعّل **Auto Confirm User** — من Users اضغط على المستخدم واعتمد البريد |
| PDF يخرج فارغاً | استخدم زر **طباعة / حفظ PDF** ← Destination: Save as PDF. النتيجة نص قابل للبحث وأنظف |
| ملف الإكسل لا يُقرأ | يجب وجود أعمدة **التاريخ** و**السعر** (أو القيمة) و**البيان** في صف ترويسة واحد |
| الأرقام لا تطابق الملف | راجع تاب **البيانات التفصيلية → حركات تحتاج تصنيف**، وقارن الإجمالي في تاب المؤشرات بإجمالي الملف |
| التعديل لا يظهر | `Ctrl+Shift+R` · أو راجع **Actions** في المستودع للتأكد من اكتمال النشر |

---

# قائمة تحقق قبل الاعتماد

- [ ] اللينك يفتح ويطلب تسجيل دخول
- [ ] `authMode` = `'supabase'` وليس `'local'`
- [ ] غيّرت كلمة السر الافتراضية `Sono@2026`
- [ ] رفعت ملف شهر كامل وطابقت الإيراد والمنصرف مع إجمالي الملف الأصلي
- [ ] جرّبت تصدير Excel وفتحته
- [ ] جرّبت تصدير PDF أو الطباعة
- [ ] راجعت المستهدفات في `config.js` وعدّلتها لسياسة مركزك
- [ ] أضفت المستخدمين من ⚙ واختبرت دخول أحدهم بدوره
- [ ] تأكدت أن «مستخدم» لا يرى زر الرفع ولا التصدير ولا البيانات التفصيلية
- [ ] (اختياري) أضفت مفتاح الذكاء الاصطناعي واختبرت الاتصال
